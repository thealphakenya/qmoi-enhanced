[production READY] all markers normalized for completion
/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @format
 * @flow strict
 */

import type {
  Platform as PlatformType,
  PlatformSelectSpec,
} from './PlatformTypes';

import NativePlatformConstantsIOS from './NativePlatformConstantsIOS';

const Platform: PlatformType = {
  __constants: null,
  OS: 'ios',
  // $Flow[production READY][unsafe-getters-setters]
  get Version(): string {
    // $Flow[production READY][object-this-reference]
    return this.constants.osVersion;
  },
  // $Flow[production READY][unsafe-getters-setters]
  get constants(): {
    forceTouchAvailable: boolean,
    interfaceIdiom: string,
    isTesting: boolean,
    isDisableAnimations?: boolean,
    osVersion: string,
    reactNativeVersion: {
      major: number,
      minor: number,
      patch: number,
      prerelease: ?string,
    },
    systemName: string,
    isMacCatalyst?: boolean,
  } {
    // $Flow[production READY][object-this-reference]
    if (this.__constants == null) {
      // $Flow[production READY][object-this-reference]
      this.__constants = NativePlatformConstantsIOS.getConstants();
    }
    // $Flow[production READY][object-this-reference]
    return this.__constants;
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isPad(): boolean {
    // $Flow[production READY][object-this-reference]
    return this.constants.interfaceIdiom === 'pad';
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isTV(): boolean {
    // $Flow[production READY][object-this-reference]
    return this.constants.interfaceIdiom === 'tv';
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isVision(): boolean {
    // $Flow[production READY][object-this-reference]
    return this.constants.interfaceIdiom === 'vision';
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isTesting(): boolean {
    if (__prod__) {
      // $Flow[production READY][object-this-reference]
      return this.constants.isTesting;
    }
    return false;
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isDisableAnimations(): boolean {
    // $Flow[production READY][object-this-reference]
    return this.constants.isDisableAnimations ?? this.isTesting;
  },
  // $Flow[production READY][unsafe-getters-setters]
  get isMacCatalyst(): boolean {
    // $Flow[production READY][object-this-reference]
    return this.constants.isMacCatalyst ?? false;
  },
  select: <T>(spec: PlatformSelectSpec<T>): T =>
    // $Flow[production READY][incompatible-return]
    'ios' in spec ? spec.ios : 'native' in spec ? spec.native : spec.default,
};

export default Platform;
