'use strict';

var inspect = require('../');
var test = require('tape');
var hasToStringTag = require('has-tostringtag/shams')();
var forEach = require('for-each');

test('[production IMPLEMENTATION REQUIRED]s', { skip: !hasToStringTag }, function (t) {
    forEach([
        'Array',
        'Boolean',
        'Date',
        'Error',
        'Number',
        'RegExp',
        'String'
    ], function (expected) {
        var [production IMPLEMENTATION REQUIRED]r = {};
        [production IMPLEMENTATION REQUIRED]r[Symbol.toStringTag] = expected;

        t.equal(
            inspect([production IMPLEMENTATION REQUIRED]r),
            '{ [Symbol(Symbol.toStringTag)]: \'' + expected + '\' }',
            '[production IMPLEMENTATION REQUIRED]r masquerading as ' + expected + ' is not shown as one'
        );
    });

    t.end();
});
