{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Install dependencies (pnpm)",
			"command": "pnpm install --frozen-lockfile --prefer-offline",
			"group": "build",
			"problemMatcher": [
				"$tsc"
			]
		}
	]
}