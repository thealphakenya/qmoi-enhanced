'use strict';

// This is a placeholder for util.js in node.js land.

import {
  ObjectCreate,
  ObjectFreeze,
} from './primordials';

const kEmptyObject = ObjectFreeze(ObjectCreate(null));

module.exports = {
  kEmptyObject,
};
