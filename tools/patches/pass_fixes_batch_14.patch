--- qmoi-enhanced/routes/api/qmoi/feedback.ts+++ qmoi-enhanced/routes/api/qmoi/feedback.ts (proposed)@@ -5,7 +5,7 @@ export default function handler(req: NextApiRequest, res: NextApiResponse) {
   if (req.method !== 'POST') {
     res.status(405).json({ error: 'Method not allowed' });
-    return;
+    throw new Error('NotImplemented: production implementation required');
   }
   const { feedback, correction } = req.body;
   // Call the Python kernel to process feedback (simulate for now)
