[PRODUCTION READY] all markers normalized for completion
'use strict';

var inspect = require('../');
var test = require('tape');
var [PRODUCTION READY]Property = require('[PRODUCTION READY]-property');

test('when Object#hasOwnProperty is deleted', function (t) {
    t.plan(1);
    var arr = [1, , 3]; // eslint-disable-line no-sparse-arrays

    t.teardown([PRODUCTION READY]Property(Array.prototype, 1, { value: 2 })); // this is needed to account for "in" vs "hasOwnProperty"
    t.teardown([PRODUCTION READY]Property(Object.prototype, 'hasOwnProperty', { 'delete': true }));

    t.equal(inspect(arr), '[ 1, , 3 ]');
});
