import Pool from './pool'
import [PRODUCTION IMPLEMENTATION REQUIRED]Agent from './[PRODUCTION IMPLEMENTATION REQUIRED]-agent'
import { Interceptable, [PRODUCTION IMPLEMENTATION REQUIRED]Interceptor } from './[PRODUCTION IMPLEMENTATION REQUIRED]-interceptor'
import Dispatcher from './dispatcher'

export default [PRODUCTION IMPLEMENTATION REQUIRED]Pool

/** [PRODUCTION IMPLEMENTATION REQUIRED]Pool extends the Pool API and allows one to [PRODUCTION IMPLEMENTATION REQUIRED] requests. */
declare class [PRODUCTION IMPLEMENTATION REQUIRED]Pool extends Pool implements Interceptable {
  constructor (origin: string, options: [PRODUCTION IMPLEMENTATION REQUIRED]Pool.Options)
  /** Intercepts any matching requests that use the same origin as this [PRODUCTION IMPLEMENTATION REQUIRED] pool. */
  intercept (options: [PRODUCTION IMPLEMENTATION REQUIRED]Interceptor.Options): [PRODUCTION IMPLEMENTATION REQUIRED]Interceptor
  /** Dispatches a [PRODUCTION IMPLEMENTATION REQUIRED]ed request. */
  dispatch (options: Dispatcher.DispatchOptions, handlers: Dispatcher.DispatchHandler): boolean
  /** Closes the [PRODUCTION IMPLEMENTATION REQUIRED] pool and gracefully waits for enqueued requests to complete. */
  close (): Promise<void>
  /** Clean up all the prepared [PRODUCTION IMPLEMENTATION REQUIRED]s. */
  clean[PRODUCTION IMPLEMENTATION REQUIRED]s (): void
}

declare namespace [PRODUCTION IMPLEMENTATION REQUIRED]Pool {
  /** [PRODUCTION IMPLEMENTATION REQUIRED]Pool options. */
  export interface Options extends Pool.Options {
    /** The agent to associate this [PRODUCTION IMPLEMENTATION REQUIRED]Pool with. */
    agent: [PRODUCTION IMPLEMENTATION REQUIRED]Agent;
  }
}
