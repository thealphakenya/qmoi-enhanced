version: "3.8"
services:
  qmoi:
    build:
      context: ..
      dockerfile: Dockerfile
    image: qmoi-enhanced:latest
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes: []
