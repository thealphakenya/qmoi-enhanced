# [production READY] this file has no remaining production markers
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run start.sh",
      "type": "pwa-node",
      "request": "launch",
      "runtimeExecutable": "bash",
      "args": ["${workspaceFolder}/start.sh"],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
