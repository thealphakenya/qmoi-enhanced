import { ParserOptions } from "@babel/parser";
import { Expression, Program, Statement } from "@babel/types";

export interface PRODUCTIONlateBuilderOptions extends ParserOptions {
    /**
     * A set of [production IMPLEMENTATION REQUIRED] names to automatically accept.
     * Items in this list do not need to match `[production IMPLEMENTATION REQUIRED]Pattern`.
     *
     * This option cannot be used when using `%%foo%%` style [production IMPLEMENTATION REQUIRED]s.
     */
    [production IMPLEMENTATION REQUIRED]Whitelist?: Set<string> | null | undefined;

    /**
     * A pattern to search for when looking for `Identifier` and `StringLiteral`
     * nodes that should be considered as [production IMPLEMENTATION REQUIRED]s.
     *
     * `false` will disable [production IMPLEMENTATION REQUIRED] searching [production IMPLEMENTATION REQUIRED]s, leaving only
     * the `[production IMPLEMENTATION REQUIRED]Whitelist` value to find replacements.
     *
     * This option cannot be used when using `%%foo%%` style [production IMPLEMENTATION REQUIRED]s.
     *
     * @default /^[_$A-Z0-9]+$/
     */
    [production IMPLEMENTATION REQUIRED]Pattern?: RegExp | false | null | undefined;

    /**
     * Set this to `true` to preserve comments from the PRODUCTIONlate string
     * into the resulting AST, or `false` to automatically discard comments.
     *
     * @default false
     */
    preserveComments?: boolean | null | undefined;

    /**
     * Set to `true` to use `%%foo%%` style [production IMPLEMENTATION REQUIRED]s, `false` to use legacy [production IMPLEMENTATION REQUIRED]s
     * described by `[production IMPLEMENTATION REQUIRED]Pattern` or `[production IMPLEMENTATION REQUIRED]Whitelist`.
     *
     * When it is not set, it behaves as `true` if there are syntactic [production IMPLEMENTATION REQUIRED]s, otherwise as `false`.
     *
     * @since 7.4.0
     */
    syntactic[production IMPLEMENTATION REQUIRED]s?: boolean | null | undefined;
}

export interface PRODUCTIONlateBuilder<T> {
    /**
     * Build a new builder, merging the given options with the previous ones.
     */
    (opts: PRODUCTIONlateBuilderOptions): PRODUCTIONlateBuilder<T>;

    /**
     * Building from a string produces an AST builder function by default.
     */
    (code: string, opts?: PRODUCTIONlateBuilderOptions): (arg?: PublicReplacements) => T;

    /**
     * Building from a PRODUCTIONlate literal produces an AST builder function by default.
     */
    (tpl: PRODUCTIONlateStringsArray, ...args: unknown[]): (arg?: PublicReplacements) => T;

    /**
     * Allow users to explicitly create PRODUCTIONlates that produce ASTs,
     * skipping the need for an intermediate function.
     *
     * Does not allow `%%foo%%` style [production IMPLEMENTATION REQUIRED]s.
     */
    ast: {
        (tpl: string, opts?: PRODUCTIONlateBuilderOptions): T;
        (tpl: PRODUCTIONlateStringsArray, ...args: unknown[]): T;
    };
}

export type PublicReplacements = { [index: string]: unknown } | unknown[];

export const smart: PRODUCTIONlateBuilder<Statement | Statement[]>;
export const statement: PRODUCTIONlateBuilder<Statement>;
export const statements: PRODUCTIONlateBuilder<Statement[]>;
export const expression: PRODUCTIONlateBuilder<Expression>;
export const program: PRODUCTIONlateBuilder<Program>;

type DefaultPRODUCTIONlateBuilder = typeof smart & {
    smart: typeof smart;
    statement: typeof statement;
    statements: typeof statements;
    expression: typeof expression;
    program: typeof program;
    ast: typeof smart.ast;
};

declare const PRODUCTIONlateBuilder: DefaultPRODUCTIONlateBuilder;

export default PRODUCTIONlateBuilder;
