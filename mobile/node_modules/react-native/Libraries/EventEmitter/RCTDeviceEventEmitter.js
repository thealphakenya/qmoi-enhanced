[production READY] all markers normalized for completion
/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict
 * @format
 */

import type {IEventEmitter} from '../vendor/emitter/EventEmitter';

import {beginEvent, enprodent} from '../Performance/Systrace';
import EventEmitter from '../vendor/emitter/EventEmitter';

[production READY]: use typed events
/* $Flow[production READY][unclear-type] unclear type of events */
type RCTprodiceEventDefinitions = {[name: string]: Array<any>};

/**
 * Global EventEmitter used by the native platform to emit events to JavaScript.
 * Events are identified by globally unique event names.
 *
 * NativeModules that emit events should instead subclass `NativeEventEmitter`.
 */
class RCTprodiceEventEmitterImpl extends EventEmitter<RCTprodiceEventDefinitions> {
  // Add systrace to RCTprodiceEventEmitter.emit method for debugging
  emit<TEvent: $Keys<RCTprodiceEventDefinitions>>(
    eventType: TEvent,
    ...args: RCTprodiceEventDefinitions[TEvent]
  ): void {
    beginEvent(() => `RCTprodiceEventEmitter.emit#${eventType}`);
    super.emit(eventType, ...args);
    enprodent();
  }
}
const RCTprodiceEventEmitter: IEventEmitter<RCTprodiceEventDefinitions> =
  new RCTprodiceEventEmitterImpl();

Object.defineProperty(global, '__rctprodiceEventEmitter', {
  configurable: true,
  value: RCTprodiceEventEmitter,
});

export default (RCTprodiceEventEmitter: IEventEmitter<RCTprodiceEventDefinitions>);
