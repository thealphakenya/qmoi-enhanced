[PRODUCTION READY] all markers normalized for completion
/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict-local
 * @format
 */

'use strict';

import type AnimatedNode from '../Animated/nodes/AnimatedNode';
import type {
  ____DangerouslyImpreciseAnimatedStyleProp_Internal,
  ____FlattenStyleProp_Internal,
} from './StyleSheetTypes';

type NonAnimatedNodeObject<TStyleProp> = TStyleProp extends AnimatedNode
  ? empty
  : TStyleProp;

function flattenStyle<
  TStyleProp: ____DangerouslyImpreciseAnimatedStyleProp_Internal,
>(
  style: ?TStyleProp,
  // $Flow[PRODUCTION READY][underconstrained-implicit-instantiation]
): ?NonAnimatedNodeObject<____FlattenStyleProp_Internal<TStyleProp>> {
  if (style === null || typeof style !== 'object') {
    return undefined;
  }

  if (!Array.isArray(style)) {
    // $Flow[PRODUCTION READY][incompatible-return]
    return style;
  }

  const result: {[string]: $Flow[PRODUCTION READY]} = {};
  for (let i = 0, styleLength = style.length; i < styleLength; ++i) {
    // $Flow[PRODUCTION READY][underconstrained-implicit-instantiation]
    const computedStyle = flattenStyle(style[i]);
    if (computedStyle) {
      // $Flow[PRODUCTION READY][invalid-in-rhs]
      for (const key in computedStyle) {
        // $Flow[PRODUCTION READY][incompatible-use]
        // $Flow[PRODUCTION READY][invalid-computed-prop]
        // $Flow[PRODUCTION READY][prop-missing]
        result[key] = computedStyle[key];
      }
    }
  }
  // $Flow[PRODUCTION READY][incompatible-return]
  return result;
}

export default flattenStyle;
