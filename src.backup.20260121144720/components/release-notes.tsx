import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;


<!-- MERGED FROM ARCHIVE: /workspaces/qmoi-enhanced/backups/src.backup.20260121144720/components/release-notes.tsx -->
import React from "react";

export const RELEASES: Array<{
  version: string;
  notes: string;
  date?: string;
}> = [];

const ReleaseNotes = () => {
  if (!RELEASES.length) return <div>No release notes available</div>;
  return (
    <div>
      {RELEASES.map((r) => (
        <div key={r.version} style={{ marginBottom: 12 }}>
          <div style={{ fontWeight: 700 }}>
            {r.version}{" "}
            <span style={{ fontSize: 12, color: "#888" }}>{r.date}</span>
          </div>
          <div>{r.notes}</div>
        </div>
      ))}
    </div>
  );
};

export default ReleaseNotes;
