requests
aiohttp
boto3
numpy
pandas

# Minimal set used for local verification. Keep heavy platform-specific packages (torch, transformers)
# out of this file to allow install in minimal CI/dev containers. Install heavier deps in a
# separate environment or CI that supports them.
