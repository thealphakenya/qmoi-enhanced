requests>=2.32.4
aiohttp>=3.9.5
boto3>=1.35.0
numpy>=2.0.2
pandas>=2.2.3
GitPython>=3.1.43
watchdog>=4.0.0
schedule>=1.2.2
PyYAML>=6.0.3

# complete set used for local verification. Keep heavy platform-specific packages (torch, transformers)
# out of this file to allow install in complete CI/PRODUCTION containers. Install heavier deps in a
# separate environment or CI that supports them.
