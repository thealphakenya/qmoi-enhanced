Current Todo Snapshot (auto-generated 2025-11-11T00:00:00Z)

Completed (automations run):
- Inventory & link checks — completed (tools/dns_docs_inventory.json, tools/dns_links_report.json).
- Conservative http->https fixes applied and merged for obvious cases.
- Scheduled link-check workflow added and will run daily.
- Dependabot enabled (weekly) and auto-merge automation added for low-risk automated PRs.

New automation added (2025-11-11):
- Vercel build/autofix workflow: `.github/workflows/vercel-autofix.yml`.
- Conservative autofix script: `tools/auto_fix_build.py` (attempts to install missing Node/Python deps and commits branch).

In-progress / next automated actions:
1) CI will run the Vercel build on push/PR. On failure it will upload build log and run `tools/auto_fix_build.py --apply` to attempt safe fixes and create a PR for review.
2) Scheduled link-check will generate proposals and create/update PRs automatically (branch: `automated/link-check-proposals`).

Manual review required (recommended):
- Review PRs created by automation before merging if you want human oversight. Low-risk http->https PRs may be auto-merged per policy.
- Review issues created for link failures in repo issues list (label: `auto/link-check`).

Files the automation updates after runs:
- `WORKSPACEGENERAL.md` — summary of automation and policies.
- `resumetodos.txt` — this file (always updated).
- `tools/link_fix_proposals.json` and `tools/link_fix_actions.md` — generated proposals.

If you request full automation, I will continue running conservative fixes, open PRs, and enable auto-merge for low-risk changes. Say "Proceed with full automation" to opt in.

Current resume snapshot generated: 2025-11-13T00:00:00Z

Recent completions (this session):
- Local QM OI dev server added: `scripts/qmoi_local_server.py` (OpenAI-style `/v1/chat/completions`).
- Persistent memory store initialized: `qmoi_memory.json`.
- Memory sync endpoints added: `/sync/push`, `/sync/pull`, `/sync/config`.
- Background auto-sync available via env `QMOI_SYNC_INTERVAL_SECONDS`.
- Standalone sync helper script added: `scripts/sync_memory.py` (supports `gist`, `hf`, and `scp` backends).
- Documentation updated: `docs/LOCAL_QMOI_DEVELOPMENT.md`, `CURLQMOIMASTERSISTERUSER.md`, `HOOKS.md`, `API.md`, `ENDPOINTS.md` references added.

Files added/updated:
- `scripts/qmoi_local_server.py` (sync endpoints + optional background sync)
- `scripts/sync_memory.py` (standalone sync helper)
- `docs/LOCAL_QMOI_DEVELOPMENT.md` (local dev server docs)
- `CURLQMOIMASTERSISTERUSER.md` (curl examples, memory sync)
- `HOOKS.md` (memory sync & production notes)
- `WORKSPACEGENERAL.md` (resume summary)

Immediate remaining tasks (high priority):
1) Configure production secrets and CI jobs:
   - Add `QMOI_GH_TOKEN` and (optionally) `QMOI_GIST_ID` as repo secrets.
   - Add `QMOI_HF_TOKEN` and `QMOI_HF_REPO` if you want HF repo sync.
2) Protect `/sync/*` endpoints with authentication before exposing the server publicly.
3) Add a scheduled CI job or cron entry that runs `python3 scripts/sync_memory.py` (or call `/sync/push`) on a cadence appropriate for your deployment.
4) Decide canonical production memory store (Postgres, Redis, object store). Implement migration plan and replace file-based memory with robust DB-backed memory service.

---
Auto-update (2025-11-13T09:28:00Z):
- `Query local qmoi for balances & memory awareness`: COMPLETED — responses saved to `logs/qmoi_balance_master.json`, `logs/qmoi_balance_sister.json`, `logs/qmoi_balance_user.json`.
- `Compare memory and md balances`: IN-PROGRESS — generated `reports/balance_memory_diff.json` and `reports/balance_memory_diff.md` (run `python3 tools/compare_memory_balances.py` to re-run).
- `Run verification using stored credentials`: NOT STARTED — requires secrets and Master approval to use or add credentials.
- `Update docs and create PR`: NOT STARTED — will annotate `MEGAVAULT.md` and create `VERIFICATION.md` after verification results.

Notes:
- The repo contains many references to Pesapal, M-Pesa and other payment APIs; verification requires their credentials (Pesapal consumer key/secret, bank API tokens, QuickBooks/Xero API tokens, etc.).
- Credentials may be stored in a local encrypted vault at `.qmoi/secrets.enc` (use `scripts/secret_store.py decrypt <passphrase>` to decrypt into `.qmoi/secrets.env`), or provided via CI secrets (`QMOI_GH_TOKEN`, `PESAPAL_CONSUMER_KEY`, etc.).
- To proceed with live verification, provide secrets via GitHub Actions secrets or a secure vault; I will not accept raw credentials pasted into chat.

Development & test suggestions:
- To test locally, run:
- To run the standalone sync helper (CI-friendly):

```bash
python3 scripts/qmoi_local_server.py &
curl -s -X POST http://localhost:8080/v1/chat/completions -H "Content-Type: application/json" -d '{"model":"qmoi","messages":[{"role":"user","content":"Hello"}]}' | jq
curl -s -X POST http://localhost:8080/sync/push -H "Content-Type: application/json" -d '{}' | jq
```

- To run the standalone sync helper (CI-friendly):

```bash
export QMOI_SYNC_BACKENDS="gist,hf"
export QMOI_GIST_ID="<gist id>"
export QMOI_GH_TOKEN="$GITHUB_TOKEN"
export QMOI_HF_REPO="user/qmoi-memory"
export QMOI_HF_TOKEN="$HF_TOKEN"
python3 scripts/sync_memory.py
```

If you want, I can now:
- A: Start the local server and run example conversations (Master/Sister/User) and show the responses and memory file updates.
- B: Create a GitHub Actions job to run `scripts/sync_memory.py` on a schedule (requires your approval to commit CI workflow updates).

Which action should I take next? (A/B or other)
- `ALLVERSIONS.md` — versions index (generated)
- `tools/versions_summary.json`, `tools/link_report.json`, `link_report.md`, `tools/autotest_report.md` (reports)

-- end
[AUDIT 2025-11-08T15:10:27.287269Z] total_files=18921 done=14707 remaining_placeholders=4151
[DONEREFS_VERIFY 2025-11-08T15:18:14.731259Z] checked=14707 kept=14596 removed=111
[DONEREFS_VERIFY 2025-11-08T15:29:10.284455Z] checked=14596 kept=14596 removed=0
