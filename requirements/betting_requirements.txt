requests==2.32.5
# psutil may require system headers to build in some environments (linux headers); keep it optional for dev.
# Remove or install psutil separately if you want system metrics: `pip install psutil`.
