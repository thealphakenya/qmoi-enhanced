# Alpha-Q-ai Merge Setup

`qmoi-enhanced-history-14` is the overall base repository for cross-repository
planning. Alpha-Q-ai retains all of its existing paths; the history base,
reachable QMOI refs, and verified Alpha history are additive merge sources.

## Source order

1. `qmoi-enhanced-history-14` base and setup conventions
2. Current `qmoi-enhanced` implementation and reachable refs
3. Alpha-Q-ai current tree and verified historical tree
4. Archived snapshots and tracker evidence

Every path must be inventoried with its owner, content identity, dependencies,
implementation role, and conflict status before it is copied or merged.

## Feature planning

The Ollama autonomous agent evaluates all source histories for measurable gaps
before proposing new sites, applications, web or mobile UI features, APIs,
backend services, platform integrations, automation, workflows, or operational
tools. It must preserve existing Alpha behavior, produce an additive plan,
run targeted and full validation, and require review for conflicts, deletions,
or new integrations.

The complete materialized Alpha source is recorded in
`FULLTREE-ALPHA-Q-AI-14.md`. The standalone remote had no reachable April 2026
commit, so the verified materialized source is identified by its exact Git ref
instead of being mislabeled as an April snapshot.

The directory `alpha-q-ai-history-14/` contains the complete 1,346-file
verified historical source tree used for comparison and recovery. It is an
evidence snapshot, not a claim that the standalone remote had an April commit.


---

## Merged source: ../Alpha-Q-ai/ALPHA_Q_AI_MERGE_SETUP.md

# Alpha-Q-ai Merge Setup

`qmoi-enhanced-history-14` is the overall base repository for cross-repository
planning. Alpha-Q-ai retains all of its existing paths; the history base,
reachable QMOI refs, and verified Alpha history are additive merge sources.

## Source order

1. `qmoi-enhanced-history-14` base and setup conventions
2. Current `qmoi-enhanced` implementation and reachable refs
3. Alpha-Q-ai current tree and verified historical tree
4. Archived snapshots and tracker evidence

Every path must be inventoried with its owner, content identity, dependencies,
implementation role, and conflict status before it is copied or merged.

## Feature planning

The Ollama autonomous agent evaluates all source histories for measurable gaps
before proposing new sites, applications, web or mobile UI features, APIs,
backend services, platform integrations, automation, workflows, or operational
tools. It must preserve existing Alpha behavior, produce an additive plan,
run targeted and full validation, and require review for conflicts, deletions,
or new integrations.

The complete materialized Alpha source is recorded in
`FULLTREE-ALPHA-Q-AI-14.md`. The standalone remote had no reachable April 2026
commit, so the verified materialized source is identified by its exact Git ref
instead of being mislabeled as an April snapshot.

The autonomous agent must inspect all live and historical files, directories,
tests, workflows, routes, symbols, configuration, and dependency boundaries
before planning work. UI features must use `STYLES.md` as their styling source
and update it when introducing a new UI pattern. Authentication, login,
identity, permissions, security, and protected-data features must use
`UNIVERSALS.md` and update it when introducing a new universal capability.
Completion requires targeted tests, the full repository validation command, and
an explicit record of skipped or blocked checks.

Each merge must execute the complete cross-repository plan: walk all files and
directories in this repository, `alpha-q-ai-history-14`, and the
`qmoi-enhanced-history-14` base; inspect all merge Markdown, tests, workflows,
routes, APIs, automation, and memory evidence; then classify Alpha-to-QMOI and
QMOI-to-Alpha feature candidates by ownership, dependencies, compatibility, and
tests. Conflicting paths remain review-blocked, and accepted changes refresh
memory indexes, awareness/identity state, automations, and workflow evidence in
both repositories.
