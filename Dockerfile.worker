FROM python:3.11-slim
WORKDIR /app
COPY . /app
ENV PYTHONUNBUFFERED=1
# Minimal runtime; no additional deps required for these scripts
CMD ["python3", "scripts/queue_worker.py", "--concurrency", "2"]
