# Placeholder Prioritization Report

This report summarizes TODO/PLACEHOLDER/SIMULATE/TODO_PROD findings and recommends priorities for production hardening.

## High Priority (blockers for production)

- Environment placeholders in `vercel.env.example` (`QMOI_API_KEY`, `M_PESA_CLIENT_ID`, `M_PESA_CLIENT_SECRET`, `TWILIO_*`, etc.) — must be guarded and reviewed before enabling real integrations.
- SMS/Push/WhatsApp notification placeholders (reports/placeholders.json references) — ensure adapters are pluggable and disabled unless PRODUCTION_CONFIRMED is set.
- Payment/wallet adapter placeholders (Mpesa, Pesapal, Binance) — implement pluggable adapters and require explicit env flags for live calls (and tests/mocks for CI).
- `TODO_PROD` stubbed endpoints and files (reports/placeholders.json) — remove or guard behind PRODUCTION_CONFIRMED or replace with functional implementations.

## Medium Priority (important but non-blocking)

- `TODO_PROD` TODOs in reports (e.g., generate real EXE, integrate with Stable Diffusion) — plan/roadmap items.
- Placeholder scanning scripts and reports — ensure they are accurate and used in CI checks.

## Low Priority (documentation / housekeeping)

- Documentation lines claiming 100% production readiness (`SESSION_COMPLETION_SUMMARY.md`) — update to reflect verified status and checklist evidence.
- Placeholder artifacts for demonstration (e.g., 169-byte EXE stub) — keep as docs but mark clearly as "placeholder".

## Immediate next actions

1. Add and enforce `PRODUCTION_CONFIRMED` env guard for any operation that performs a live external call or could have side effects (payments, SMS, push, deploying/redeploying, file downloads/executables).
2. Implement adapter interfaces for SMS, Push, Payments with no-op mock implementations used by default in non-production (PRODUCTION_CONFIRMED!=true).
3. Replace in-code `// Simulate` blocks with guarded simulation only enabled when `NODE_ENV !== 'production' && PRODUCTION_CONFIRMED !== 'true'`.
4. Add CI check job: `scan-placeholders` that fails the build if `TODO`/`TODO_PROD` tokens exist in non-doc files unless explicitly whitelisted.

---

Generated by the automation agent on request; next step is to implement runtime guards and start replacing high-priority placeholders.
